import sanityClient from '@sanity/client'

export default sanityClient({
    projectId:"8oix0qv6",//every project that u have will have projectID in in sanity website and in Portfolio manager
    dataset:"production"

})